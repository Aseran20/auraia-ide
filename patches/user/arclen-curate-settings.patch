diff --git a/src/vs/workbench/contrib/preferences/browser/settingsEditor2.ts b/src/vs/workbench/contrib/preferences/browser/settingsEditor2.ts
index c754843..11575f7 100644
--- a/src/vs/workbench/contrib/preferences/browser/settingsEditor2.ts
+++ b/src/vs/workbench/contrib/preferences/browser/settingsEditor2.ts
@@ -777,6 +777,12 @@ export class SettingsEditor2 extends EditorPane {
 		const targetWidgetContainer = DOM.append(headerControlsContainer, $('.settings-target-container'));
 		this.settingsTargetsWidget = this._register(this.instantiationService.createInstance(SettingsTargetsWidget, targetWidgetContainer, { enableRemoteSettings: true }));
 		this.settingsTargetsWidget.settingsTarget = ConfigurationTarget.USER_LOCAL;
+		// Arclen: hide the User / Workspace / Remote / Folder scope switcher. Analysts edit ONE
+		// consistent config (User scope, the default) — per-workspace setting overrides are a
+		// developer concept that adds "why is this different here?" confusion. The widget stays
+		// instantiated and pinned to USER_LOCAL; workspace settings still LOAD if present (engine
+		// intact) — only the GUI scope-switcher is removed (still editable via .vscode/settings.json).
+		targetWidgetContainer.style.display = 'none';
 		this._register(this.settingsTargetsWidget.onDidTargetChange(target => this.onDidSettingsTargetChange(target)));
 		this._register(DOM.addDisposableListener(targetWidgetContainer, DOM.EventType.KEY_DOWN, e => {
 			const event = new StandardKeyboardEvent(e);
@@ -1493,7 +1499,15 @@ export class SettingsEditor2 extends EditorPane {
 			}
 		}
 
-		resolvedSettingsRoot.children!.push(await createTocTreeForExtensionSettings(this.extensionService, extensionSettingsGroups, filter));
+		// Arclen Strict: keep ONLY first-party / bundled extensions in the curated Settings nav
+		// (Claude Code today; future Arclen-bundled ids go here). Every other installed extension's
+		// settings stay fully searchable but out of the browse tree — deny-by-default cockpit.
+		const arclenKeptExtensionIds = ['anthropic.claude-code'];
+		const keptExtensionSettingsGroups = extensionSettingsGroups.filter(group =>
+			arclenKeptExtensionIds.some(id => group.extensionInfo?.id?.toLowerCase() === id.toLowerCase()));
+		if (keptExtensionSettingsGroups.length) {
+			resolvedSettingsRoot.children!.push(await createTocTreeForExtensionSettings(this.extensionService, keptExtensionSettingsGroups, filter));
+		}
 
 		resolvedSettingsRoot.children!.unshift(getCommonlyUsedData(groups));
 
diff --git a/src/vs/workbench/contrib/preferences/browser/settingsLayout.ts b/src/vs/workbench/contrib/preferences/browser/settingsLayout.ts
index 5d1c11f..23de23a 100644
--- a/src/vs/workbench/contrib/preferences/browser/settingsLayout.ts
+++ b/src/vs/workbench/contrib/preferences/browser/settingsLayout.ts
@@ -27,20 +27,19 @@ export interface ITOCEntry<T> {
 	hide?: boolean;
 }
 
+// Arclen: analyst-focused "Commonly Used" — the first group an M&A analyst sees when
+// opening Settings. Replaced the upstream dev-leaning defaults (formatOnSave, defaultFormatter,
+// tabSize, files.exclude…) with theme/font/save + the Claude Code (chat.*) knobs that matter
+// for the cockpit. Engine untouched — any other setting is still reachable via search/JSON.
 const COMMONLY_USED_SETTINGS: readonly string[] = [
+	'workbench.colorTheme',
 	'editor.fontSize',
-	'editor.formatOnSave',
-	'files.autoSave',
-	'GitHub.copilot-chat.manageExtension',
-	'editor.defaultFormatter',
 	'editor.fontFamily',
 	'editor.wordWrap',
-	'chat.agent.maxRequests',
-	'files.exclude',
-	'workbench.colorTheme',
-	'editor.tabSize',
 	'editor.mouseWheelZoom',
-	'editor.formatOnPaste'
+	'files.autoSave',
+	'chat.fontSize',
+	'chat.agent.maxRequests'
 ];
 
 export function getCommonlyUsedData(settingGroups: ISettingsGroup[]): ITOCEntry<ISetting> {
@@ -70,10 +69,35 @@ export const tocData: ITOCEntry<string> = {
 	id: 'root',
 	label: 'root',
 	children: [
+		{
+			// Arclen: the curated, analyst-first section. Placed FIRST so it consumes its
+			// settings from the pool (first-match-wins, settingsTree.ts) — they render here with
+			// native widgets and do NOT duplicate under the native categories below. This is the
+			// substrate for "Arclen Preferences": real settings, read by the engine, zero custom UI.
+			id: 'arclen',
+			label: localize('arclenCategory', "Arclen"),
+			children: [
+				{
+					id: 'arclen/appearance',
+					label: localize('arclenAppearance', "Appearance"),
+					settings: ['workbench.colorTheme', 'editor.fontSize', 'editor.fontFamily', 'editor.mouseWheelZoom', 'window.zoomLevel']
+				},
+				{
+					id: 'arclen/deliverables',
+					label: localize('arclenDeliverables', "Files & Deliverables"),
+					settings: ['files.autoSave', 'files.autoSaveDelay', 'editor.wordWrap']
+				},
+				{
+					id: 'arclen/assistant',
+					label: localize('arclenAssistant', "Assistant"),
+					settings: ['chat.fontSize', 'chat.agent.maxRequests']
+				}
+			]
+		},
 		{
 			id: 'editor',
 			label: localize('textEditor', "Text Editor"),
-			settings: ['editor.*'],
+			settings: ['editor.*'], hide: true, /* Arclen Strict: raw dev editor tree hidden from nav — analyst essentials live in Arclen/Commonly Used; everything still reachable via search/JSON */
 			children: [
 				{
 					id: 'editor/cursor',
@@ -125,7 +149,7 @@ export const tocData: ITOCEntry<string> = {
 		{
 			id: 'workbench',
 			label: localize('workbench', "Workbench"),
-			settings: ['workbench.*'],
+			settings: ['workbench.*'], hide: true, /* Arclen Strict */
 			children: [
 				{
 					id: 'workbench/appearance',
@@ -150,24 +174,24 @@ export const tocData: ITOCEntry<string> = {
 				{
 					id: 'workbench/zenmode',
 					label: localize('zenMode', "Zen Mode"),
-					settings: ['zenmode.*']
+					settings: ['zenmode.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'workbench/screencastmode',
 					label: localize('screencastMode', "Screencast Mode"),
-					settings: ['screencastMode.*']
+					settings: ['screencastMode.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'workbench/browser',
 					label: localize('browser', "Browser"),
-					settings: ['workbench.browser.*']
+					settings: ['workbench.browser.*'], hide: true /* Arclen */
 				}
 			]
 		},
 		{
 			id: 'window',
 			label: localize('window', "Window"),
-			settings: ['window.*'],
+			settings: ['window.*'], hide: true, /* Arclen Strict */
 			children: [
 				{
 					id: 'window/newWindow',
@@ -178,7 +202,7 @@ export const tocData: ITOCEntry<string> = {
 		},
 		{
 			id: 'chat',
-			label: localize('chat', "Chat"),
+			label: localize('chat', "Chat"), hide: true, /* Arclen Strict: raw 100+ chat knobs hidden from nav; the Claude Code extension's own settings stay browsable under Extensions, key knobs curated in Arclen/Assistant, rest searchable */
 			children: [
 				{
 					id: 'chat/agent',
@@ -287,7 +311,7 @@ export const tocData: ITOCEntry<string> = {
 		},
 		{
 			id: 'features',
-			label: localize('features', "Features"),
+			label: localize('features', "Features"), hide: true, /* Arclen Strict: whole Features tree hidden from nav (Explorer/Search/Terminal/Debug/…); searchable */
 			children: [
 				{
 					id: 'features/accessibilitySignals',
@@ -312,22 +336,22 @@ export const tocData: ITOCEntry<string> = {
 				{
 					id: 'features/debug',
 					label: localize('debug', "Debug"),
-					settings: ['debug.*', 'launch']
+					settings: ['debug.*', 'launch'], hide: true /* Arclen: dev surface hidden from Settings nav — engine intact, still reachable via search/JSON */
 				},
 				{
 					id: 'features/testing',
 					label: localize('testing', "Testing"),
-					settings: ['testing.*']
+					settings: ['testing.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/scm',
 					label: localize('scm', "Source Control"),
-					settings: ['scm.*']
+					settings: ['scm.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/extensions',
 					label: localize('extensions', "Extensions"),
-					settings: ['extensions.*']
+					settings: ['extensions.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/terminal',
@@ -337,27 +361,27 @@ export const tocData: ITOCEntry<string> = {
 				{
 					id: 'features/task',
 					label: localize('task', "Task"),
-					settings: ['task.*']
+					settings: ['task.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/problems',
 					label: localize('problems', "Problems"),
-					settings: ['problems.*']
+					settings: ['problems.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/output',
 					label: localize('output', "Output"),
-					settings: ['output.*']
+					settings: ['output.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/comments',
 					label: localize('comments', "Comments"),
-					settings: ['comments.*']
+					settings: ['comments.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/remote',
 					label: localize('remote', "Remote"),
-					settings: ['remote.*']
+					settings: ['remote.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/timeline',
@@ -367,12 +391,12 @@ export const tocData: ITOCEntry<string> = {
 				{
 					id: 'features/notebook',
 					label: localize('notebook', 'Notebook'),
-					settings: ['notebook.*', 'interactiveWindow.*']
+					settings: ['notebook.*', 'interactiveWindow.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/mergeEditor',
 					label: localize('mergeEditor', 'Merge Editor'),
-					settings: ['mergeEditor.*']
+					settings: ['mergeEditor.*'], hide: true /* Arclen */
 				},
 				{
 					id: 'features/issueReporter',
@@ -384,7 +408,7 @@ export const tocData: ITOCEntry<string> = {
 		},
 		{
 			id: 'application',
-			label: localize('application', "Application"),
+			label: localize('application', "Application"), hide: true, /* Arclen: whole Application category (Proxy/Keyboard/Update/Telemetry/Sync/Network/Experimental) hidden */
 			children: [
 				{
 					id: 'application/http',
@@ -432,7 +456,7 @@ export const tocData: ITOCEntry<string> = {
 		{
 			id: 'security',
 			label: localize('security', "Security"),
-			settings: ['security.*'],
+			settings: ['security.*'], hide: true, /* Arclen: Security/Workspace-Trust hidden (trust already disabled) */
 			children: [
 				{
 					id: 'security/workspace',
